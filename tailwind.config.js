/** @type {import('tailwindcss').Config} */
export default {
  // Dark-only site; class-based mode is not needed but harmless. Keeping default.
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
};
